# checkpoint schema

| field | description |
|---|---|
| `id` | local source identifier |
| `captured_utc` | normalized capture time |
| `status` | review state |

Identifiers are internal and are not geographic codes.
